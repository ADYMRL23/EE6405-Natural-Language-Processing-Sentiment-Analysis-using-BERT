create environment with the below required libraries:

    pip install streamlit pandas numpy scikit-learn transformers torch matplotlib nltk

once environment is setup, enter environment and run ui with:

    streamlit run app.py

artifacts folder is an example of required folder structure to be able to load models with example bert demo